import Shop from "./Shop";

const NewArrivals = () => (
  <Shop
    title="New Arrivals"
    eyebrow="Just In"
    description="The latest pieces from our atelier — crafted in limited runs."
    filterTag="New"
  />
);

export default NewArrivals;
